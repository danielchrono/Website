/// <reference types="node" />
declare class IdentityProvider {
    issuer: string;
    privateKey?: Buffer;
    certificate?: Buffer;
    expiresInMinutes: number;
    constructor({ issuer, privateKey, certificate, expiresInMinutes }: {
        issuer: string;
        privateKey?: Buffer;
        certificate?: Buffer;
        expiresInMinutes?: number;
    });
}
export { IdentityProvider };
